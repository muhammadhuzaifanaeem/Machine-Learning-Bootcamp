# Quiz – Day 3

1. What is the difference between a Series and a DataFrame?
Answer: Series show a one line where DataFrame shows rows and columns in a table formate.

2. Which function loads a CSV file?
Answer: pd.read_csv("----")

3. What does `head()` do?
Answer: It shows the values of DF. Its default value is 5.

4. What does `shape` return?
Answer: It returns number of rows and columns.

5. Which function shows missing values?
Answer: df.isnull()

6. What does `dropna()` do?
Answer: It drop the rows with null values.

7. What does `fillna()` do?
Answer: It fills the missing values.

Score: 7 / 7